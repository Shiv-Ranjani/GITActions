name: dotnet package

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: Shiv-Ranjani/GITActions/.github/workflows/helloworld.yaml@main
      - name: Welcome
        run: echo "Welcome!!!"
